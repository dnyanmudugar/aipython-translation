# translationai-python
This python module allows you to translate text to another language using neural network. It does have its own module, and does not include third-party module like googletrans library. It uses CSV file format as well as model trainings and tokenizer.
